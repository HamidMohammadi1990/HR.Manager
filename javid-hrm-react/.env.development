# Backend API (Edition.Api / JavidHrm.Api)
# Leave empty to use Vite dev-server proxy (/api → backend)
VITE_API_BASE_URL=

# Used by Vite proxy when VITE_API_BASE_URL is empty
VITE_API_PROXY_TARGET=http://localhost:5000
